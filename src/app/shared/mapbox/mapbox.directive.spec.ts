import { MapboxDirective } from './mapbox.directive';

describe('MapboxDirective', () => {
  it('should create an instance', () => {
    const directive = new MapboxDirective();
    expect(directive).toBeTruthy();
  });
});
